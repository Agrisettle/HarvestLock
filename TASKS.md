# Task backlog

The granular, living breakdown behind `SPRINT.md`'s day-by-day schedule and `ROADMAP.md`'s long-horizon plan. `SPRINT.md` says *when*; `ROADMAP.md` says *why and in what phase*; this file says *exactly what*, task by task, with enough detail that picking one up doesn't require asking what it means. Check items off as they land, and keep descriptions accurate to what actually got built — same rule as everywhere else in this project.

Not GitHub issues yet — this is the shared reference we work from directly before anything gets filed publicly.

---

## Contracts (`HarvestLock-Contracts`)

Current state: happy-path state machine + claimable-balance-with-expiry + mutual cancellation + buyer-position assignability + two-phase funding + buyer-default/seller-non-delivery forfeiture + PRD §7 shortfall/grade adjustment + PRD §4.8/§16.1 allocation ledger, 76/76 tests, testnet-verified seven times. Next, in priority order (matches the contracts repo's own `HANDOFF.md`):

- [x] **Allocation ledger** — built 4 Sept 2026: `set_allocation(members: Vec<AllocationMember>)` records each member farmer's entitlement as a **per-member salted hash** (never a bare phone-number hash — stronger than the per-contract floor this item asked for) plus a `share_bps`, cooperative-gated, one-time, callable only from `Draft`. Deliberately not required before `lock` — a solo-farmer commitment with no cooperative pooling shouldn't be forced through a step that doesn't apply to it. Both sub-task decisions below resolved: settlement math is **record-only** (PRD §4.9 already states this as the v1 default explicitly, not an open question — `settle` doesn't pro-rate), and the off-chain identity map is built (`api/src/db/allocationMembers.ts`, migration `005_allocation_members.sql`) — the salt+phone-number mapping lives only there, which is what makes the on-chain hash genuinely erasable per NDPA s.34 (`DELETE /allocation-members/:memberHash`). 9 new contract unit tests (76/76 total) plus 70/70 API tests including a full live end-to-end scenario through the real HTTP layer. Live-verified on testnet (Deployment 7): `set_allocation`/`get_allocation` round-tripped exactly, a second `set_allocation` call correctly rejected on-chain, `lock` confirmed to still work with an allocation already recorded.
- [x] **Real attestation-driven settlement** — built 3 Sept 2026: `confirm_delivery` now takes `delivered_quantity: u32, grade_index: u32` instead of being a bare boolean gate; `initialize` gained `contracted_quantity: u32` and a pre-agreed `grade_price_bps: Vec<u32>` table (grade -> price multiplier in bps, fixed at initialize, never renegotiated at settlement). `settle` pays the cooperative only what's still owed against the computed `settlement_bps` (already-claimed advances never clawed back, per PRD's "advance not clawed back" partial-delivery handling) and refunds the rest of the funded remainder to the buyer as a shortfall credit — a real behavior change, not just additive. 9 new contract unit tests (67/67 total), live-verified on testnet: a 50%-quantity/90%-grade delivery produced real on-chain transfers of exactly the hand-computed amounts. API side: `initialize` gained `contractedQuantity`/`gradePriceBps` params; `confirm_delivery` moved to its own route (`POST .../tx/confirm-delivery`, single-signer) since the generic no-arg builder can't serve an args-taking method — see `api/HANDOFF.md`. On-chain data shape decided: plain numbers attested by the warehouse operator, no oracle needed, matching the PRD's own framing of grade/quantity disputes as the operator's own appeals process, not a HarvestLock dispute.
- [ ] **Oracle staleness handling** — PRD §16.3. Currently unbuilt: no NGN/oracle conversion exists at all yet (§4.2 is entirely deferred). This becomes real once denomination work starts; not urgent before that.
- [x] **Assignability** — buyer position transfer with cooperative consent, recorded on-chain. Deliberately not a market (no order book/listing) — a novation, not a trade. Built 2 Sept 2026 as `reassign_buyer()`: three-party-consented (outgoing buyer, cooperative, incoming buyer — one more signer than "with cooperative consent" alone requires, on purpose). 34/34 unit tests, live-testnet-verified with three genuinely different signers via `api/test/helpers.ts`, including a functional proof that reclaim rights transfer to the new buyer.
- [x] **Cancellation / mutual unwind** — PRD §7's defined unwind: advance settled per agreed schedule, remaining escrow returned, no penalty, logged. Built 1 Sept 2026 as `cancel()`: buyer+cooperative co-signed, reachable Draft through Advance2Released. 30/30 unit tests, plus live-testnet-verified with two genuinely different signers via `api/test/helpers.ts` — see `HarvestLock-Contracts/HANDOFF.md`. A real frontend UX for triggering it is a separate, still-open item — see `api/HANDOFF.md`'s "Next steps."
- [x] **Buyer default / side-selling forfeiture paths** — built 2 Sept 2026 as two-phase funding (`lock` now escrows only the deposit) plus `ready_for_delivery()`/`fund_remainder()`/`expire_remainder_window()`/`reclaim_on_nondelivery()`. Buyer default: `expire_remainder_window()`, permissionless, sweeps escrow to the cooperative, `Status::Defaulted` — per this session's product decision, carries an *immediate* off-chain permanent bar (not graduated), contingent on prior disclosure and an appeal channel (email, see below). Seller non-delivery: `reclaim_on_nondelivery()`, buyer-gated, returns escrow to the buyer, `Status::Forfeited` — carries a *graduated* 3-strike system before a bar, reflecting that the seller genuinely can't access escrowed funds unilaterally. Both are uncontested-by-construction deadline checks; `Status::Disputed` remains unbuilt (needs a real arbitration mechanism this session didn't design). 58/58 unit tests, three-scenario live-testnet verification (happy path, buyer-default sweep by an unrelated third-party signer, seller-non-delivery reclaim) — see `HarvestLock-Contracts/HANDOFF.md`. API wiring, off-chain reputation/strikes tracking, the `buyer-app` consent checkbox, and the `site/` Roles & Responsibilities doc are all done too (see `api/`, `buyer-app/`, and `site/` sections below) — only the appeals inbox's actual review process (`samuelojetunde898@gmail.com`, user-supplied) remains open, deliberately (human-reviewed, not automated).
- [x] **`claim_window_secs` minimum/maximum** — enforced API-level per the documented lean (contract itself still has no floor/ceiling, deliberately — see contracts HANDOFF.md item 5). `api/src/server.ts`: 1 hour floor, 90 day ceiling, rejected with 400 before a transaction is even built. Engineering defaults, not researched values — revisit once there's real pilot behavior to observe.

## API (`api/`) — core lifecycle real and testnet-verified; identity/ledger work still ahead

Target shape per PRD §17: TypeScript/Node, Postgres, talks to the deployed Soroban contract via `@stellar/stellar-sdk`, deploys SDP for farmer payouts (later, not week 1).

- [x] **Project scaffold** — package.json, TypeScript config, Fastify (chosen over a heavier framework, matching this project's bias against unnecessary dependencies).
- [x] **Postgres schema, v1** — `commitments` table (migration `001_init.sql`), mirrors on-chain state, cached from chain reads, not the source of truth. `allocation_members` (migration `005_allocation_members.sql`) added 4 Sept 2026 — see below.
- [x] **Stellar SDK connection layer** — `src/stellar/{client,deploy,tx}.ts`. Covers reads (`get_status`, `get_commitment`) and every write (`initialize`, `reassign_buyer`, `confirm_delivery` — each with its own route since each takes arguments — plus every remaining no-arg lifecycle method — `lock`/`release_advance_*`/`claim_advance_*`/`reclaim_advance_*`/`mark_checkpoint`/`settle`/`cancel`/`ready_for_delivery`/`fund_remainder`/`expire_remainder_window`/`reclaim_on_nondelivery`, generically). Breadth achieved: every contract function is reachable. `cancel`/`reassign_buyer` needed a genuinely different signing path (multi-party Soroban auth, two and three parties respectively) — see `api/test/helpers.ts`. The four two-phase-funding/forfeiture additions (2 Sept 2026, later same day) needed nothing extra — all single-signer or permissionless. `confirm_delivery` moved out of the generic no-arg set on 3 Sept 2026 once it started taking `delivered_quantity`/`grade_index` (PRD §7 shortfall/grade adjustment) — single-signer, so no multi-party staging needed, just its own route.
- [x] **REST endpoints** wrapping the above — `src/server.ts`. Deploy, build-tx-per-method, generic signed-submit, live read, cached list. No auth, and the data model doesn't assume a single user (see `api/README.md`).
- [x] **Tests against testnet** — `api/test/stellar.test.ts`, not mocked: SDK-level lifecycle (deploy → initialize → lock), a full HTTP-layer walk run by hand on 1 Sept 2026, and a genuine two-different-signer `cancel` call, all confirmed via fresh chain reads.
- [x] **`api/README.md` and `api/HANDOFF.md`** — both written 1 Sept 2026, describing what's real, what's deferred, and why (build-unsigned/client-signs/submit architecture, deploy-vs-initialize split, cache-refresh-on-read model, the `cancel` multi-party exception).
- [x] **`allocation_members` table + off-chain identity map** — built 4 Sept 2026: `src/db/allocationMembers.ts`, per-*member* random salts (`HMAC-SHA256(salt, phoneNumber)`, matching exactly what goes on-chain). `POST .../tx/set-allocation` stages the salt+hash and builds the unsigned XDR without persisting yet; `/transactions/submit`'s `allocationContractId`/`allocationMembers` only writes the phone-number mapping once the on-chain call is confirmed, so a built-but-never-submitted proposal can't leave an orphaned PII row. `DELETE /allocation-members/:memberHash` is the actual NDPA s.34 erasure endpoint. Live-verified end to end through the real HTTP layer (build → sign → submit → on-chain read → off-chain Postgres read → erase → on-chain read unaffected).
- [x] **A real "create commitment" UX flow** — built 2 Sept 2026 in `buyer-app` (`CreateCommitmentForm.tsx`): deploy → build initialize → Freighter signs → submit → load. Client-side validation mirrors the API's own bounds (advance bps sum, claim window) so a bad value fails fast. What the three-call shape actually surfaced, as predicted: nothing awkward in the shape itself, but the flow is unreachable in this environment's real-browser checks since it's gated behind wallet connection and no real Freighter extension exists here — see `buyer-app/README.md`.
- [x] **Two-phase funding + buyer-default/seller-non-delivery forfeiture wired into the API** — built 2 Sept 2026, later same day: `ready_for_delivery`/`fund_remainder`/`expire_remainder_window`/`reclaim_on_nondelivery` added to the generic no-arg route; `initialize` gained `remainderWindowSecs`/`deliveryWindowSecs` with their own API-level bounds. `test/stellar.test.ts` proves all three new terminal outcomes live (26/26 tests). See `api/HANDOFF.md`.
- [x] **Off-chain reputation/strikes schema + the `initialize`-time bar check** — built 2 Sept 2026, later same day again: `party_standing`/`standing_events` (migration `002_reputation.sql`), `src/reputation.ts`/`src/db/reputation.ts`. Buyer default = immediate bar, idempotent; cooperative forfeiture = a real 3-strike counter. `applyReputationConsequences` fires exactly once per fresh transition into `Defaulted`/`Forfeited`, wired into both `upsertCommitment` call sites in `server.ts`; `requireNotBarred` rejects a barred buyer/cooperative on `initialize` with 403. New `GET /parties/:address/standing`. 39/39 tests overall — a new real-Postgres suite (`test/reputation.test.ts`, 10 tests) plus two extended live-testnet scenarios proving the full chain-read → cache → consequence loop. Deliberately observation-triggered, not backed by a chain indexer — same caveat the commitments cache already carries. See `api/HANDOFF.md`.
- [ ] **The appeals process itself** — the contact (`samuelojetunde898@gmail.com`) is documented and live on `site/roles.html`; there's no inbox automation or reinstatement workflow behind it yet, deliberately (matches this project's bias against over-building before there's real usage). Reinstating a barred address today means a human updating `party_standing` by hand.
- [x] **A real multi-party signing UX for `cancel`** — built 2 Sept 2026, later same day, third time: staged propose/sign/finalize (`src/stellar/multiParty.ts`, migration `003_pending_cancellations.sql`, since generalized — see below). Either party proposes; the *other* signs their own Soroban auth entry from their own wallet (Freighter's `signAuthEntry`, not `signTransaction`) in a separate request, at a separate time, from a separate device — no script or process ever holds both keys. The proposer finalizes through the *existing* `/transactions/submit`, deliberately not a new endpoint. Decided the design question `api/HANDOFF.md` had flagged (server-side staging store vs. client-side QR/link hand-off) in favor of server-side, since the API already coordinates everything else both frontends do. Frontend UI (`CancelSection.tsx`) landed the same day too — see `coop-pwa`/`buyer-app` sections.
- [x] **The same staged-signing UX for `reassign_buyer`, API side** — built 3 Sept 2026: generalized the mechanism (`src/db/pendingMultisigProposals.ts`, migration `004_generalize_multisig_proposals.sql`, renaming `pending_cancellations` → `pending_multisig_proposals` + a `method` column) rather than duplicating it, since `multiParty.ts`'s propose/finalize were already generic enough (just needed an `args` param for `new_buyer`) and the sign route needed no method-awareness at all (a proposal ID is already unique) — only propose/read stayed per-method, since each has a different "who's allowed to propose" rule: `reassign_buyer` restricts proposing to the *current* buyer specifically (PRD §4.8 frames it as the outgoing buyer's own decision), unlike `cancel`'s either-party rule. 50/50 tests overall, including a full live three-party walk through the real HTTP layer (propose rejected from an unrelated party *and* from the cooperative, then propose → cooperative signs → incoming buyer signs → finalize → submit → buyer field genuinely changed, confirmed via a fresh chain read). Frontend UI (`ReassignBuyerSection.tsx`) landed the same day too — see `coop-pwa`/`buyer-app` sections.

## `coop-pwa/` — dashboard + first write action built, tested; real-wallet QA/auth/offline still ahead

- [x] Read-only dashboard: show a commitment's current status, the state-machine position, advance-tranche claim windows and deadlines. Built 1 Sept 2026, checked in a real browser against the live API and live testnet — see `coop-pwa/README.md` for what that check found (a real CORS bug in the API, fixed there).
- [x] Automated tests — vitest + Testing Library added 1 Sept 2026, covering `StatusBadge`, `CommitmentDetail`'s formatting logic, and `App`'s fetch-mocked flows. `npm test` runs in CI now too.
- [ ] Phone-based auth flow — no seed phrases surfaced to cooperative users (PRD §4.6). Needs the API's identity/session model decided first.
- [x] Claim-advance action (write, not just read) — built 2 Sept 2026 via Freighter (testnet/MVP stand-in, not the real phone-auth model). Build → sign → submit → refresh, API/component logic tested (13 tests). Real gap, flagged not hidden: no real Freighter extension available in this environment, so actual wallet signing hasn't been manually QA'd — see `coop-pwa/README.md`.
- [x] "Cancel this commitment" UI — built 2 Sept 2026, later same day again, as `src/components/CancelSection.tsx`: propose/approve/finalize, wired to the API's propose/sign/finalize routes. `wallet.ts` gained `signAuthEntry` (distinct from `signTransactionXdr` — signs one auth entry, not a transaction). Polls the active proposal every 10s while waiting on the other party. 7 new component tests. Same "no real Freighter extension in this environment" caveat as the claim-advance action above — the API side of this flow is separately verified live end to end, see `api/HANDOFF.md`.
- [x] "Propose reassignment" UI for `reassign_buyer` — built 3 Sept 2026, as `src/components/ReassignBuyerSection.tsx`: same propose/approve/finalize shape as `CancelSection.tsx`, generalized to a form (collects the new buyer's address, since only the current buyer may propose) and two pending signers instead of one. `justApproved` local state covers the gap where the API's `pending_entries` stops naming a signer once they've signed — not solved for page-reload recovery, a deliberate, documented gap. 7 new component tests; `App.test.tsx`'s fetch-mock chains updated for the new background poll, same fix shape `CancelSection`'s rollout needed.
- [x] Offline-tolerant queue for the depot connectivity-loss case (PRD §7/§16.3) — built 4 Sept 2026: `vite-plugin-pwa` precaches the app shell (installable, loads with no network); `src/offlineQueue.ts` (IndexedDB) queues a claim's *intent* — not a pre-signed transaction, since the API's 60-second tx timeout and Stellar's sequence-number model both make a stale queued signature unreliable — when a build request can't reach the network at all (`isOfflineError`, a `fetch` `TypeError`). `OfflineQueueBanner.tsx` surfaces queued claims with a manual "Retry" (deliberately not auto-retried on an `online` event — see `coop-pwa/README.md`). 5 new tests against real (in-memory) IndexedDB via `fake-indexeddb`, 2 new `App.test.tsx` scenarios.

## `buyer-app/` — dashboard + lock/settle/create-commitment built, tested; real-wallet QA/auth/ERP still ahead

- [x] Read-only dashboard: same information as coop-pwa's, buyer-facing framing (what they've locked, what's pending, settlement status). Built and browser-checked 1 Sept 2026 — see `buyer-app/README.md`.
- [x] Automated tests — same setup as coop-pwa, plus specific coverage for `pendingSummary`'s buyer-framing branches. `npm test` runs in CI now too.
- [x] Lock/settle actions -- built 2 Sept 2026 via Freighter (testnet/MVP stand-in). settle() confirmed permissionless (any connected wallet, tested with a non-buyer/non-cooperative address) since the contract has no require_auth() on it. Real gap, flagged not hidden: no real Freighter extension available in this environment -- see buyer-app/README.md.
- [x] Two-phase funding fields + Roles & Responsibilities consent — built 2 Sept 2026, later same day: `CreateCommitmentForm` collects `remainderWindowSecs`/`deliveryWindowSecs` (defaults 7 days/120 days), same client-side-bounds-mirroring pattern as `claimWindowSecs`. A required checkbox blocks submission until the buyer acknowledges `site/roles.html` (opt-in link via `VITE_SITE_URL`, degrades to plain text when unset). Enforced client-side only — the API can't verify a UI actually showed this. 46/46 tests passing (7 new).
- [x] "Cancel this commitment" UI — same item as `coop-pwa`'s (see there), built 2 Sept 2026, later same day again — identical component, since either party can propose or approve a cancellation from either app.
- [x] "Propose reassignment" UI for `reassign_buyer` — same item as `coop-pwa`'s (see there), built 3 Sept 2026 — identical component, though this is the app where the propose form actually matters, since only the *current* buyer may initiate a reassignment.
- [ ] ERP integration — explicitly out of scope until there's a real off-taker to integrate with (PRD §16.2); don't build this speculatively.

## `site/` — small, unblocked, good starting points for a new contributor

- [ ] Wordmark lockup (icon + "Agrisettle" text) for use in docs/README headers — mentioned as a quick follow-up in `BRANDING.md`, never built.
- [x] Accessibility pass beyond the contrast check already done: skip-to-content link and a real `:focus-visible` style added 1 Sept 2026 (neither existed — no `:focus` rule anywhere), verified in a real browser including the "activating the skip link needs `tabIndex={-1}` on the target, not just the anchor" gotcha. `aria-hidden` on the decorative mark SVGs checked — was already correct, nothing to fix.
- [x] Open Graph image — added 1 Sept 2026 (`site/public/og-image.png`, on-brand, regeneratable from `site/og-image-source.html`). Uses a relative path since there's no deployed domain yet; fix to absolute once there is one.
- [x] Live indicator on the Status section — done 1 Sept 2026, `useLiveStatus` hook + a badge on the "Testnet contract" link, opt-in via `VITE_API_URL` (unset by default, gracefully renders nothing rather than erroring — there's no deployed public API yet). Verified in a real browser both with and without the env var set.
- [x] Roles & Responsibilities disclosure page — built 2 Sept 2026, later same day, as `roles.html` (a second Vite multi-page entry, no router dependency needed). Explains the two-phase funding flow, what counts as a buyer default vs. a cooperative forfeiture, default window lengths, and the appeals email — the advance-notice requirement behind this session's default/forfeiture product decisions. Linked from the footer and from `buyer-app`'s create-commitment consent checkbox.

---

## How this file is meant to be used

Pick an unblocked task, say so before starting on anything nontrivial (avoids duplicate work), build it with the same rigor as everything shipped so far — real tests, testnet verification where applicable, HANDOFF.md updated in the same PR, not a follow-up that may never happen. Check the box here when it's actually done, not when it's started.
