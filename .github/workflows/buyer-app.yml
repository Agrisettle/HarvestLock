name: buyer-app

on:
  push:
    branches: [main]
    paths:
      - "buyer-app/**"
      - ".github/workflows/buyer-app.yml"
  pull_request:
    paths:
      - "buyer-app/**"
      - ".github/workflows/buyer-app.yml"

jobs:
  build:
    name: typecheck + lint + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: buyer-app
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: buyer-app/package-lock.json

      - name: npm ci
        run: npm ci

      - name: typecheck
        run: npx tsc -b --noEmit

      - name: lint
        run: npm run lint

      - name: build
        run: npm run build
